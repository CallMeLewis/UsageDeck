This beta improves refresh reliability when one or more enabled providers cannot load usage.

## Fixes

- Refreshing several providers at once no longer closes UsageDeck when one provider quickly reports a sign-in requirement, missing setup or another usage warning. Other enabled providers continue refreshing and the warning remains visible.

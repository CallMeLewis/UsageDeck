<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="UsageDeck.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="using:H.NotifyIcon"
    Title="UsageDeck">
    <Grid x:Name="RootLayout">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="SolidBackground"
            Grid.RowSpan="2"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            IsHitTestVisible="False" />

        <Grid
            x:Name="WindowDragRegion"
            Height="32"
            Background="Transparent">
            <StackPanel
                Margin="12,0,144,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="8">
                <Image
                    Width="16"
                    Height="16"
                    AutomationProperties.AccessibilityView="Raw"
                    Source="ms-appx:///Assets/AppIcon.png" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="12"
                    FontWeight="SemiBold"
                    Opacity="0.72"
                    Text="UsageDeck" />
            </StackPanel>
        </Grid>

        <Frame
            x:Name="RootFrame"
            Grid.Row="1"
            IsNavigationStackEnabled="False" />

        <tb:TaskbarIcon
            x:Name="TrayIcon"
            ContextMenuMode="PopupMenu"
            IconSource="Assets/AppIcon.ico"
            LeftClickCommand="{x:Bind ToggleWindowCommand}"
            MenuActivation="RightClick"
            ToolTipText="UsageDeck usage"
            Visibility="Collapsed">
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout Opening="TrayContextMenu_Opening">
                    <MenuFlyoutItem Command="{x:Bind ShowWindowCommand}" Text="Show usage" />
                    <MenuFlyoutItem Command="{x:Bind RefreshCommand}" Text="Refresh" />
                    <MenuFlyoutSubItem x:Name="NotificationActionsMenuItem" Text="Pause notifications">
                        <MenuFlyoutItem
                            Command="{x:Bind PauseNotificationsForThirtyMinutesCommand}"
                            Text="For 30 minutes" />
                        <MenuFlyoutItem
                            Command="{x:Bind PauseNotificationsForOneHourCommand}"
                            Text="For 1 hour" />
                        <MenuFlyoutItem
                            Command="{x:Bind PauseNotificationsForTwoHoursCommand}"
                            Text="For 2 hours" />
                        <MenuFlyoutItem
                            Command="{x:Bind PauseNotificationsForFourHoursCommand}"
                            Text="For 4 hours" />
                        <MenuFlyoutItem
                            Command="{x:Bind PauseNotificationsUntilTomorrowCommand}"
                            Text="Until tomorrow morning" />
                        <MenuFlyoutSeparator />
                        <MenuFlyoutItem
                            x:Name="ResumeNotificationsMenuItem"
                            Command="{x:Bind ResumeNotificationsCommand}"
                            IsEnabled="False"
                            Text="Resume notifications" />
                    </MenuFlyoutSubItem>
                    <MenuFlyoutItem Command="{x:Bind SettingsCommand}" Text="Settings" />
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Command="{x:Bind ExitCommand}" Text="Exit" />
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</Window>
